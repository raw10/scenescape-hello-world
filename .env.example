# SceneScape Hello World Environment Configuration
# Copy this file to .env.local and set your actual SceneScape values
# For Docker: docker-compose will automatically load .env.local
# For native Python: source .env.local or set these as environment variables

# SceneScape Server Configuration
SCENESCAPE_REST_URL=https://your-scenescape-host/api/v1
SCENESCAPE_API_TOKEN=your_api_token_here

# MQTT Broker Configuration  
SCENESCAPE_MQTT_HOST=your-scenescape-host
SCENESCAPE_MQTT_PORT=1883

# SSL/TLS Configuration (usually false for development)
SCENESCAPE_VERIFY_SSL=false

# Authentication file path (contains MQTT credentials)
SCENESCAPE_AUTH_FILE=secrets/controller.auth

# Alternative: Direct MQTT credentials (instead of auth file)
# SCENESCAPE_MQTT_USER=scenectrl
# SCENESCAPE_MQTT_PASS=your_password_here

# Example with IP addresses (replace with your actual values):
# SCENESCAPE_REST_URL=https://10.1.63.50/api/v1
# SCENESCAPE_MQTT_HOST=10.1.63.50
